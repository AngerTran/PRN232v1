# Sao chép file này thành .env và chỉnh sửa cho phù hợp môi trường.
# URL gốc của Backend API. Để trống khi FE và BE cùng domain (nginx proxy /api).
# Local dev (FE :5173, BE :3001): bắt buộc set URL BE.
VITE_API_BASE_URL=http://localhost:3001
