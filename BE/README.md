# BE — ASP.NET Core Web API

REST API: auth (email + Google OAuth), profiles, Supabase + PostgreSQL.

## Chạy

```bash
dotnet restore
dotnet run
```

## Cấu hình

Copy `appsettings.Development.example.json` thành `appsettings.Development.json` và điền Supabase / Google OAuth.
